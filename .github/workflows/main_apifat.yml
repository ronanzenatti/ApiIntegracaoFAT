# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - apifat

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Build with dotnet
        run: dotnet build --configuration Release

      - name: dotnet publish
        run: dotnet publish -c Release -o ${{env.DOTNET_ROOT}}/myapp

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp
          retention-days: 1
          compression-level: 6

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      # ==================================================================
      # ALTERAÇÃO 1: FAZER CHECKOUT DO CÓDIGO FONTE
      # Adicionamos este passo para que o job tenha acesso à estrutura de arquivos do projeto.
      - name: Checkout repository
        uses: actions/checkout@v4
      # ==================================================================

      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app
          path: ./deployment
        timeout-minutes: 5

      # CORREÇÃO: Verificar se o download funcionou
      - name: Verify artifact download
        run: |
          echo "📁 Conteúdo do diretório deployment:"
          ls -la ./deployment/
          if [ -z "$(ls -A ./deployment/)" ]; then
            echo "❌ ERRO: Nenhum arquivo foi baixado!"
            exit 1
          fi
          echo "✅ Artifact baixado com sucesso"
        
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_F06C8115B4764B4FB4B6AD28104D37BE }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_4501437E89344FE9BE8FB80C4A9DA5E0 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_16B262A420D745628D34D72E3B8001FA }}

      # CORREÇÃO 3: Instalar .NET SDK novamente no job de deploy e usar caminho correto
      - name: Set up .NET Core for migrations
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

       # DIAGNÓSTICO: Testar conectividade com MySQL
      - name: Test MySQL Connectivity
        run: |
          echo "🔍 Testando conectividade com MySQL Azure..."
          
          # Instalar mysql client
          sudo apt-get update
          sudo apt-get install -y mysql-client
          
          # Testar conexão básica (sem senha para não expor no log)
          echo "Testando se o servidor está acessível..."
          if timeout 10s mysql -h api-bd.mysql.database.azure.com -P 3306 -u api -p'P#ssw0rdr' --ssl-mode=REQUIRED -e "SELECT 1;" 2>/dev/null; then
            echo "✅ Conexão MySQL funcionando"
          else
            echo "❌ Falha na conexão MySQL"
            echo "Testando conectividade básica (ping)..."
            ping -c 3 api-bd.mysql.database.azure.com || echo "Ping falhou"
            
            echo "Testando porta 3306..."
            nc -zv api-bd.mysql.database.azure.com 3306 || echo "Porta 3306 não acessível"
          fi

      # ==================================================================
      # Este passo foi adicionado para instalar as ferramentas do EF Core
      # e executar as migrations no banco de dados do Azure antes do deploy.
      # Utiliza um secret do repositório para a string de conexão.
      - name: Run EF Core Migrations
        run: |
          echo "🔄 Instalando EF Core tools..."
          dotnet tool install --global dotnet-ef
          
          echo "📁 Estrutura atual dos arquivos:"
          ls -la
          ls -la ./deployment/ || echo "Deployment não encontrado"
          ls -la ApiIntegracao/ || echo "ApiIntegracao não encontrado"
          
          echo "🔄 Executando migrations..."
          
          # CORREÇÃO: Usar o projeto do código fonte (não o deployment)
          # O EF precisa dos arquivos .cs e .csproj, não apenas os binários
          dotnet ef database update \
            --project ApiIntegracao/ApiIntegracao.csproj \
            --startup-project ApiIntegracao/ApiIntegracao.csproj \
            --connection "server=api-bd.mysql.database.azure.com;port=3306;database=api_integracao;user=api;password=P#ssw0rdr;SslMode=Required;Connect Timeout=30;Command Timeout=60;AllowPublicKeyRetrieval=true;" \
            --verbose
          
          echo "✅ Migrations executadas com sucesso"
      # FIM DA ALTERAÇÃO
      # ==================================================================

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'apifat'
          slot-name: 'Production'
          package: ./deployment

          


